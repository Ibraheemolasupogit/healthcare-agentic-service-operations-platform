# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
*.egg

# --- Virtual environments ---
.venv/
venv/
env/

# --- Test / coverage / type-check caches ---
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# --- Environment / secrets ---
.env
.env.*
!.env.example
*.pem
*.key

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- Generated artefacts (kept out of version control; folders kept via .gitkeep) ---
# reports/ is transient/reproducible by default (regenerate via
# `python -m business_process.evidence` / `python -m power_platform.evidence` /
# `python -m ai.evaluation`); the explicitly unignored JSON reports are
# intentionally tracked as portfolio evidence — see reports/README.md.
outputs/*
!outputs/.gitkeep
!outputs/README.md
reports/*
!reports/.gitkeep
!reports/README.md
!reports/case_summary.json
!reports/automation_summary.json
!reports/agentic_ai_evaluation_summary.json

# --- Docker ---
*.log
