# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
*.egg

# --- Virtual environments ---
.venv/
venv/
env/

# --- Test / coverage / type-check caches ---
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# --- Environment / secrets ---
.env
.env.*
!.env.example
*.pem
*.key

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- Generated artefacts (kept out of version control; folders kept via .gitkeep) ---
# reports/ is transient/reproducible by default (regenerate via
# `python -m business_process.evidence`); case_summary.json is the one
# generated report we intentionally track as portfolio evidence — see
# reports/README.md for the full rule.
outputs/*
!outputs/.gitkeep
!outputs/README.md
reports/*
!reports/.gitkeep
!reports/README.md
!reports/case_summary.json

# --- Docker ---
*.log
